首先创建一个数组
通过一个for循环，长度100多
然后通过random()产生随机数来产生几等奖

通过getImageData获取到当前画布的数据，将数据转换成rgba形式，
再转换成16进制的形式 #xxx
通过计算子字符串在总字符串中出现的次数
子字符串（先前定义好canvas的背景色16进制#xxx）
总字符串（整个canvas当前数据的rgba形式的16进制）
当涂抹的区域比例大于0.5的时候，弹出获得奖项

